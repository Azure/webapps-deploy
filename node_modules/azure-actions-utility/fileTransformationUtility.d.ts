import { PackageType } from './packageUtility';
export declare class FileTransformUtility {
    private static rootDirectoryPath;
    static applyTransformations(webPackage: string, parameters: string, packageType: PackageType): Promise<string>;
}
