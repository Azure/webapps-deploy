import { IAuthorizationHandler } from "./IAuthorizationHandler";
export declare class AzCliAuthHandler implements IAuthorizationHandler {
    private static endpoint;
    private _subscriptionID;
    private _baseUrl;
    private token;
    private constructor();
    static getEndpoint(param?: string): AzCliAuthHandler;
    readonly subscriptionID: string;
    readonly baseUrl: string;
    getToken(force?: boolean): any;
}
