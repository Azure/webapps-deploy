"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.SiteContainer = void 0;
class SiteContainer {
    constructor(name, image, targetPort, isMain, authType, userName, passwordSecret, userManagedIdentityClientId) {
        this.name = name;
        this.image = image;
        this.targetPort = targetPort;
        this.isMain = isMain;
        this.authType = authType;
        this.userName = userName;
        this.passwordSecret = passwordSecret;
        this.userManagedIdentityClientId = userManagedIdentityClientId;
    }
    getName() {
        return this.name;
    }
    getImage() {
        return this.image;
    }
    getTargetPort() {
        return this.targetPort;
    }
    getIsMain() {
        var _a;
        return (_a = this.isMain) !== null && _a !== void 0 ? _a : false;
    }
    getAuthType() {
        return this.authType;
    }
    getUserName() {
        return this.userName;
    }
    getPasswordSecret() {
        return this.passwordSecret;
    }
    getUserManagedIdentityClientId() {
        return this.userManagedIdentityClientId;
    }
    setName(name) {
        this.name = name;
    }
    setImage(image) {
        this.image = image;
    }
    setTargetPort(targetPort) {
        this.targetPort = targetPort;
    }
    setIsMain(isMain) {
        this.isMain = isMain;
    }
    setAuthType(authType) {
        this.authType = authType;
    }
    setUserName(userName) {
        this.userName = userName;
    }
    setPasswordSecret(passwordSecret) {
        this.passwordSecret = passwordSecret;
    }
    setUserManagedIdentityClientId(userManagedIdentityClientId) {
        this.userManagedIdentityClientId = userManagedIdentityClientId;
    }
    static fromJson(item) {
        var _a, _b;
        return new SiteContainer(item.name, item.image, (_a = item.targetPort) === null || _a === void 0 ? void 0 : _a.toString(), (_b = item.isMain) !== null && _b !== void 0 ? _b : false, item.authType, item.userName, item.passwordSecret, item.userManagedIdentityClientId);
    }
}
exports.SiteContainer = SiteContainer;
