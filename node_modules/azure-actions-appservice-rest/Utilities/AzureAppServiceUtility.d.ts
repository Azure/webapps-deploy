import { AzureAppService } from '../Arm/azure-app-service';
import { Kudu } from '../Kudu/azure-app-kudu-service';
export declare class AzureAppServiceUtility {
    private _appService;
    private _webClient;
    constructor(appService: AzureAppService);
    getWebDeployPublishingProfile(): Promise<any>;
    getApplicationURL(virtualApplication?: string): Promise<string>;
    pingApplication(): Promise<void>;
    pingApplicationWithUrl(applicationUrl: string): Promise<void>;
    /**
     * Gets a Kudu service client for deployments.
     * @param warmupInstanceId Optional instance ID for SPN auth to pin all requests to a single instance (ARRAffinity cookie)
     */
    getKuduService(warmupInstanceId?: string): Promise<Kudu>;
    /**
     * Gets the first available instance ID for warmup cookie pinning.
     * Used with SPN authentication to ensure all requests go to the same instance.
     * Sorts instances by name and picks the first one (similar to az-cli behavior).
     * @returns Instance ID string, or undefined if not available
     */
    getWarmupInstanceId(): Promise<string | undefined>;
    updateConfigurationSettings(properties: any): Promise<void>;
    updateAndMonitorAppSettings(addProperties?: any, deleteProperties?: any): Promise<boolean>;
    updateConnectionStrings(addProperties: any): Promise<boolean>;
}
