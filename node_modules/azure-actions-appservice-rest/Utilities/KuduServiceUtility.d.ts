import { Kudu } from '../Kudu/azure-app-kudu-service';
export declare class KuduServiceUtility {
    private _webAppKuduService;
    private _deploymentID;
    constructor(kuduService: Kudu);
    updateDeploymentStatus(taskResult: boolean, DeploymentID: string, customMessage: any): Promise<string>;
    getDeploymentID(): string;
    deployUsingZipDeploy(packagePath: string, customMessage?: any): Promise<string>;
    deployUsingRunFromZip(packagePath: string, customMessage?: any): Promise<void>;
    deployUsingWarDeploy(packagePath: string, customMessage?: any, targetFolderName?: any): Promise<string>;
    deployUsingOneDeploy(packagePath: string, customMessage?: any, targetPath?: any, type?: any, clean?: any, restart?: any): Promise<string>;
    postZipDeployOperation(oldDeploymentID: string, activeDeploymentID: string): Promise<void>;
    /**
     * Warms up the Kudu service using the dedicated warmup endpoint.
     * For Publish Profile: Cookie is captured from response headers automatically.
     * For SPN: Pass warmupInstanceId to getKuduService() to pre-set the ARRAffinity cookie.
     */
    warmUp(): Promise<void>;
    warmpUp(): Promise<void>;
    private _processDeploymentResponse;
    private _printZipDeployLogs;
    private _getUpdateHistoryRequest;
    deployWebAppImage(appName: string, images: string, isLinux: boolean): Promise<void>;
}
