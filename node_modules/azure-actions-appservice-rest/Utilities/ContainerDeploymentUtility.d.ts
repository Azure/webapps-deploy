import { AzureAppService } from '../Arm/azure-app-service';
export declare class ContainerDeploymentUtility {
    private _appService;
    private _appServiceUtility;
    constructor(appService: AzureAppService);
    deployWebAppImage(images: string, multiContainerConfigFile: string, isLinux: boolean, isMultiContainer: boolean, startupCommand: string): Promise<void>;
    private _updateImagesInConfigFile;
    private _tokenizeImages;
    private _updateConfigurationDetails;
}
