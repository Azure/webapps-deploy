import { WebRequest, WebRequestOptions, WebResponse } from 'azure-actions-webclient/WebClient';
export declare class KuduServiceClient {
    private _scmUri;
    private _accesssToken;
    private _cookie;
    private _webClient;
    constructor(scmUri: string, accessToken: string);
    beginRequest(request: WebRequest, reqOptions?: WebRequestOptions, contentType?: string): Promise<WebResponse>;
    getRequestUri(uriFormat: string, queryParameters?: Array<string>): string;
    getScmUri(): string;
}
